#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define sz(c) (int)c.size()
#define all(c) c.begin(),c.end()
#define f first
#define s second

// If wins are equal, both aren't winners
// if one person won more than the other, eliminate the other
// I think u need to split it down into modules which u can build
// upwards in less queries

void solve() {
	int n;
	cin >> n;

}

int main(void) {
	int t;
	cin >> t;
	for (int i = 1; i <= t; ++i)
		solve();
}


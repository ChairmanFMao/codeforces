#include <bits/stdc++.h>
using namespace std;
#define ll long long

bool difference(pair<int,int> f, pair<int,int> s) {
	return f.second - f.first < s.second - s.first;
}

// Unsure how to remove the extra one if the number if characters is odd
// Very annoyed because I spent ages on this problem

void solve() {
	string s;
	cin >> s;
	int out = 0;
	
	
}

int main(void) {
	ios::sync_with_stdio(0);
	cin.tie(0);
	
	cout << "\n";
	int t;
	cin >> t;
	for (int i = 1; i <= t; ++i)
		solve();
}


#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define sz(c) (int)c.size()
#define all(c) c.begin(),c.end()

// Test time limit is 4 seconds
// trying to make as big as possible with positive sum
// For each total just check when the next positive rise is
// Looked in comments and saw mentions of dp and a segment tree, idk tho
// The tags are data structures and dp
// Segments trees can get sums of parts of an array in O(log n) time
// Unsure how that would be helpful in this context

void solve() {
	int n;
	cin >> n;
	vector<int> a(n);
	for (int& i : a)
		cin >> i;

	
}

int main(void) {
	ios::sync_with_stdio(0);
	cin.tie(0);
	cout << "\n";
	int t;
	cin >> t;
	for (int i = 1; i <= t; ++i)
		solve();
}


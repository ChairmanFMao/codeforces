#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define sz(c) (int)c.size()
#define all(c) c.begin(),c.end()
#define f first
#define s second

// if connection is 1, then an u+v == even
// otherwise with connection 0, then u+v == odd
// this is to maintain connections
// however we might not want to do this when making the graph
// connected

void solve() {
	
}

int main(void) {
	ios::sync_with_stdio(0);
	cin.tie(0);
	
	cout << "\n";
	int t;
	cin >> t;
	for (int i = 1; i <= t; ++i)
		solve();
}


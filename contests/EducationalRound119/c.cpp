#include <bits/stdc++.h>
using namespace std;
#define ll long long

// The numbers in this problem are massive and I'm not really sure how I'm going to do it

void solve() {
    ll n, k, x;
    cin >> n >> k >> x;
    string s;
    cin >> s;


}

int main(void) {
    ios::sync_with_stdio(0);
    cin.tie(0);

    cout << "\n";
    int t;
    cin >> t;
    for (int i = 1; i <= t; ++i)
        solve();
}

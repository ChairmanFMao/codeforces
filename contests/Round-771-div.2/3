
#include <bits/stdc++.h>
using namespace std;
#define ll long long

const int mxN = 1000;
int n, m, currentMove = 0;
vector<vector<bool>> used(mxN+1, vector<bool> (mxN+1, 0));
vector<vector<int>> grid(mxN+1, vector<int> (mxN+1));
vector<vector<int>> moves(mxN+1, vector<int> (3, 0));

void solve() {
    cin >> n >> m;
    for (int i = 0; i < n; i++)
        for (int j = 0; j < m; j++)
            cin >> grid[i][j];
}

int main(void) {
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    solve();
}


#include <bits/stdc++.h>
using namespace std;
#define ll long long

// Looked at solution and I am trying to decypher the logic

void solve() {
    int n;
    cin >> n;
    vector<int> b(n), p(n);
    for (int i = 0; i < n; i++)
        cin >> b[i];
    for (int i = 0; i < n; i++)
        cin >> p[i];
    
    
}

int main(void) {
    ios::sync_with_stdio(0);
    cin.tie(0);

    cout << "\n";
    int t;
    cin >> t;
    for (int i = 1; i <= t; ++i)
        solve();
}
